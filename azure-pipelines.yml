
trigger:
- master

pool:
  vmImage: ubuntu-latest

stages:
  - stage:
    displayName: 'Nunit'
    jobs:
      - job: 'NunitTest'
        displayName: 'Nunit Test'
        steps:
          - script: echo Hello, world!
          - task: DotNetCoreCLI@2
            inputs:
              command: 'test'
